
class Solution(object):
    def isValid(self, strs:s) -> bool:
        stack = []
        bracket_map = {')':'(', '}':'{', ']':'['}

        for char in s:
            bracket_map[char] in 
        


sol = Solution()
s = "()"
ouput = sol.isValid(s)
print(ouput)
